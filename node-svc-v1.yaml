apiVersion: v1
kind: Service
metadata:
  name: node-app-cb-lb
  namespace: default
spec:
  type: LoadBalancer

  selector:
    app: node-app-cb

  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
